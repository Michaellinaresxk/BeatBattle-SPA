export interface MainCategory {
  id: string;
  name: string;
  icon: string;
  description: string;
  color: string;
  path: string;
}
export interface Category {
  id: string;
  name: string;
  icon?: string;
  color?: string;
}
